import React from 'react';
import './Loading.css'; // Adicione o caminho correto para o arquivo CSS

const Loading = () => (
  <div className="loading-overlay">
    <div className="spinner"></div>
  </div>
);

export default Loading;